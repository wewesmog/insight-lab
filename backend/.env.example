# SQLite path (default: backend/data/insight_lab.db)
# DATABASE_PATH=backend/data/insight_lab.db

# Optional: enable LLM analysis (otherwise uses rule-based engine — great for demos)
# OPENAI_API_KEY=sk-...
# LLM_MODEL=gpt-4o-mini
